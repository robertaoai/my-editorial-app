# B-086 — D-200 needs a complete tool and handoff bootstrap contract

- **Raised:** 2026-09-08 by Lane B
- **Kind:** spec-defect
- **Phase:** 1
- **Blocks:** independent approval of D-200's bootstrap as complete and ready for implementation; no unrelated intake or product-plan work
- **Status:** Answered
- **Lane A:** **Acknowledged and answered `D-201`, 2026-09-08 — all four children stand, none dismissed, and one of them corrects a claim `D-200` had no business making.** **Parent 1 applied:** `.githooks/`, `docs/graph-fragments/*.js`, Graphify runtime operations, `.gitattributes`, `.gitignore`, `postcss.config.mjs` and root `README.md` are **Claude Code's**; `docs/graph-fragments/*.json` is **Cowork's** as curated meaning — a second exact-file exception alongside `.agents/rules/graphify.md`, and exact-file exceptions now apply **before** directory rules. **Operating a tool is not editing it** — the distinction `D-200` never drew, and the reason the Graphify pipeline is Code's while fragment meaning is Cowork's. **Child 2 accepted in full and it is worse than you stated it:** the classifier is `ENTRY_FILE` in `handoff-fields.mjs:130`, imported by `handoff-response`, `closure-readiness` **and the fixture runner**; `channel-docs.mjs:123` carries a **separate inverse** pattern, so an un-widened `A-` file is not unread — it is **misclassified as a channel instruction document**. `D-200` named one reader of four. **Child 2's ordering correction is the sharpest finding and is recorded as such.** My claim that documenting the series first *would fail the suite* is **false and withdrawn**: `channel-docs` validates fields, resolutions and tallies, not filename series, and your read-only counterexample returned no findings. **A control asserted to fire and never tested — the same error as the `source-sweep` invocation that could not run, and the subject of `D-199`.** Replaced by your activation rule: **readers, documentation, run registration and fixtures agree before any `A-` entry is filed**, and **never edit a checker to make a predicted failure come true.** **`D-200`'s blanket prohibition is withdrawn** — it blocked `A-001` itself; the receipt contract is recorded, and **run assignment in §5.0a is your order 4, owed, not claimed.** **Child 3 accepted:** `lane-gate.mjs` returns early on a one-lane change and a Cowork/Code crossing *is* one, so lane and tool crossings must evaluate independently; it reports paths, never an inferred actor (`D-87`). **Channel activation and tool crossing are separate controls.** **Child 4 accepted and the runbook is withdrawn entirely** — bare `merge7.js` merges `frag7.json` only, `--fill-missing` does not complete semantics, `stale: false` is not currency, and **`git gc --prune=now` is not lock cleanup**. **The push is complete; my credential-blocked claim is stale.** **This answer is `Applied`, not `Verified` — it is Lane A answering a critic record of Lane A's own act, and `D-201` needs its own separate-turn review.**
- **Evidence:** independent review at a6ce25315c89260503c2149b7c16c029bd3bde95 of D-201's partial source correction and outstanding B-086 criteria; earlier review at 73226b7759920348a5ba04915ba6ad506bcf700c; D-200/D-201; shared core; Build Spec; handoff readers and channel SOP; Phase Closure run/report contract; installed Graphify/Git source and documentation; local consistency and fresh remote observations below

## Independent review of D-201 — 2026-09-08

**Clarified request:** verify the source correction against this entry's existing criteria, identify
what is actually complete, and draft the remaining parent-first fixes. Reviewed and applying
revision: `a6ce25315c89260503c2149b7c16c029bd3bde95`. This review neither applies the governed
corrections nor builds the planned controls.

### Tracking correction and bounded acceptance

Lane A's response is present and substantive, so this entry's **Status is now Answered**. It was
still Open in the reviewed commit, despite the response saying the answer was Applied. D-201's
Register/shared-core changes are **partly applied at a named commit**; this whole entry still has
unmet source and implementation criteria. No `Resolution`, `Verified-By` or whole-entry verification
claim is added. An acknowledged reply, partial source application and terminal closure remain
different facts. The original Lane A narrative above is preserved.

**Accepted within D-201's applied scope:** the additional tool ownership and path exceptions;
Code operating Graphify while Cowork owns curated meaning; Code-to-Cowork return distinguished
from a lane boundary; receiver/producer and registered-run contract; receipt separated from
verification; withdrawal of the false README-order guarantee and unsafe terminal runbook; and
channel activation separated from tool-crossing enforcement. These improvements do not require
the future implementation to be recognized as correct source changes.

### Remaining parent gap — source propagation, not premature implementation

| Existing criterion | Current evidence | Draft correction / success criterion |
|---|---|---|
| Build Spec carries the corrected ownership, bounded sequence and DoD | §2:59 still maps all docs except handoffs to Cowork and points only to D-200. D-201's own tier table says the Build Spec update is **not yet** applied | Replace that existing paragraph with the proposed text below. The source packet is complete only when its operative execution document agrees with D-201 |
| Inventory applicability is explicit | D-201's tier table omits the Inventory column/disposition that Parent 1 requested | Record a reasoned unaffected disposition for the source-only correction if no artifact lifecycle changes. When the actual tooling packet creates/retires a file, name it and propagate then; no new inventory or backlog file |
| Completion claim matches the remaining work | D-201 says it completes order 1, while acknowledging the outstanding Build Spec propagation | Use: "Register and shared-core corrections applied; Parent 1 awaits Build Spec propagation and Inventory applicability. Implementation and channel activation retain their separate later gates" |
| Planned next work has an authority/scope boundary | D-201 explicitly authorizes no build; ownership and an ordered list do not establish a covering tooling-execution act | Identify any existing act covering the exact tooling unit and reuse it. If none covers it, present the completed bounded packet once at that boundary. Do not turn this source review into implementation authority |

**Proposed replacement for the existing Build Spec §2 paragraph — not applied:**

> Lane A uses two tools within one lane, one commit lock and one live Active row, under D-200
> as corrected by D-201. The Register/shared core own the full path map, including the rule-file,
> executable graph-tool and curated-meaning exceptions; this paragraph does not maintain another
> map. Cowork owns the source contract and Claude Code implements its bounded tooling units.
> The channel unit covers the shared A/B/C entry classifier, its receipt/closure/fixture consumers
> and channel-document exclusion, with A-case and existing B/C regression evidence. Its activation
> DoD is agreement among readers, channel documentation, assigned unique report run and fixtures.
> A Code-to-Cowork return is distinct from a Lane A boundary report; the latter keeps its Register
> record. Tool-crossing classification and trailer enforcement form a separate unit, evaluated
> independently of lane crossings and not required merely to activate the return channel.
> Each implementation unit must identify its exact paths, exclusions, DoD and covering act before
> execution. After the final source/implementation changes, verify graph currency, claimed semantic
> scope and curated parity, then run the complete local suite. These planned units confer no product
> build, S2 release, lane transition or deployment authority.

The existing Child 2 and Child 3 sections below remain the detailed candidate paths and acceptance
cases for those two units. No new work-order or duplicate status ledger is proposed.

**Inventory draft:** "Unaffected by this Register/shared-core/Build Spec source correction:
no implementation artifact is created or retired by it. The later bounded implementation packet
must state its own file lifecycle and update the Inventory if that lifecycle changes." This does
not assert that every future tooling change will be inventory-unaffected.

### Two source-history corrections and one graph-evidence correction

- D-200 did have an exit condition: widen the reader and document the series before filing A
  entries. Replace D-201's "with no exit" account with **"the earlier activation condition covered
  too little of the reader, documentation, run and fixture contract."** Keep D-201's fuller rule;
  do not invent a historical deadlock to justify it.
- D-199's subject is the AT-001 application/coverage dispute. For the earlier non-executing
  source-sweep command, cite B-084's review; for the false channel-docs guarantee, cite this entry's
  counterexample. Correct that attribution without reopening D-199's accepted partial repair.
- The measured `graph-coverage` failure names **B-086 only**. D-201 is a section of the existing
  Register; B-084 is an already represented file. Their changed content requires a currency/semantic
  review, but they are not additional missing-file findings. Graph operations now have their Code
  owner; any necessary change to tracked curated meaning still comes from Cowork before ingestion.

### Lane A follow-up — preserve the actual dependency order

| Order / parent | Next action | Accept when / follow-up phase |
|---|---|---|
| 1 / source correction, Cowork | Finish the existing Build Spec paragraph and Inventory applicability; correct the completion/provenance statements above | Phase 1: bounded source packet agrees with D-201, without claiming tooling is implemented |
| 2 / complete source packet, Claude Code | Implement the channel unit within its covering authority and verify the existing Child 2 cases | Phase 1 tooling: shared recognition and all consumers work; B/C behavior preserved |
| Independent child / complete source packet, Claude Code | Implement tool classification/gate within its own covering scope and verify Child 3 cases | Phase 1 tooling: lane/tool controls evaluated separately; no inferred actor attribution |
| 3 / channel readers ready, Cowork | Complete current-use SOP/template/routing clauses and assign the actual return run in §5.0a | Phase 1 activation: these prerequisites agree before A-001 is filed. Actual run assignment may remain at this later stage; it is not needed to recognize the earlier source corrections |
| 4 / final consuming revision, Code | Synchronize the final docs/graph state using the installed workflow, preserve curated meaning and measure all local checks | Phase 1 evidence: exact analyzed HEAD, semantic scope and coverage. Refresh interim source evidence when consumed; do not wait for unrelated product work |
| 5 / completed relevant criteria, independent reviewer | Review the resulting revision and record the supported disposition here | This entry is not Verified from D-201's partial application or a turn report alone; source-only acceptance remains explicitly bounded |

Product PRD, storyboard panels, product UML/data flow, traceability map and Encyclopedia source
files are unchanged by `a6ce253`. The development-tool correction does not settle B-084's contract
or view follow-ups. B-084 remains Applied; B-085's independent bounded verification is preserved.
B-071/D-171, legacy handoffs and the Phase 3 deferrals retain their own owners and gates.

**Review evidence:** a fresh fetch finds upstream at `73226b7`, local HEAD at `a6ce253`, one outgoing
commit and no divergence. The local suite reports 15/17: only docs-drift and graph-coverage fail.
Graph metadata still names `78cd2b3`; `check-update` saying "looks current" does not override that
HEAD mismatch. No graph write, governed-source edit, implementation, commit or push was performed
in this pass. Only this handoff's response status, evidence and review text were updated.

| Verdict | Tier / item | Follow-up phase |
|---|---|---|
| Approve | D-201's bounded ownership, activation, receipt and control corrections | Phase 1: preserve the applied source evidence at a6ce253 |
| Approve-with-conditions | Parent 1 source-packet completion | Phase 1 Cowork: Build Spec propagation, Inventory applicability and precise completion claims |
| Defer | Channel implementation, separate tool gate, activation/run assignment and whole B-086 closure | Existing Phase 1 units and independent-review gates |
| Reject | Whole-parent completion ahead of source propagation; reply text as lifecycle metadata; graph-current claim from one signal | Correct the source/reporting claims without reopening settled product decisions |

## What happened

**Clarified request:** independently review D-200's tool split and the quoted terminal runbook,
keep B-084's intake repair separate, and draft a parent-first correction with testable completion
criteria. Planning only: no implementation, new lane, A-series entry, build or deployment.

D-200 correctly keeps one Lane A, one lock and one live lane-state row, while assigning strategy
to Cowork and implementation to Claude Code. Reusing the existing handoff channel is sound.
Its implementing contract is incomplete, and several claimed mechanical guarantees are false.
This entry is the requested separate critic record; its existence does not verify D-200 or the
future implementation. [B-084](B-084-d197-manual-intake-specification-reconciliation.md) retains
the D-199/AT-001 review and its open contract/view work. B-085 remains independently Verified.

## What you need

### Parent 1 — complete ownership and the report contract

| Gap / source | Draft correction | Accept when |
|---|---|---|
| The "all surfaces" map omits `.githooks/`, although item 3 changes its hook. It also leaves Graphify runtime operations unassigned and puts executable `docs/graph-fragments/*.js` under the general Cowork docs rule | Apply T2 explicitly: propose `.githooks/`, the existing merge/coverage JavaScript tools and Graphify runtime operations to Claude Code; curated semantic JSON and governing prose stay Cowork's. Classify the remaining existing lane-map root files, including `.gitattributes`, `.gitignore`, `postcss.config.mjs` and root README, explicitly or through an unambiguous category. Preserve the rule-file exception under `.agents/` | Every actual bootstrap path has a tool owner. Cowork owning the schema/meaning of graph fragments does not prevent Code from operating the agreed merge workflow; running a tool is distinguished from editing its source |
| The return path is not yet distinguished from Lane A's existing boundary report | Define **Code→Cowork tool return** separately from **Lane A→another lane boundary**. Amend the current blanket A-series exclusions in docs README and Phase Closure §5.2 narrowly, preserving D-138's Register record for lane boundaries | The two report events have explicit purposes; no duplicate report or new lane-state row |
| A-001 has no assigned run or complete receipt/verification contract | Assign the actual implementation-return run in Phase Closure §5.0a before filing its report. `Kind: turn-report`, `Phase: 1`, filled Evidence and unique registered Run; omit Resolution, Verified-By and Verified-At-Commit. The existing `Lane A` response field names Cowork as receiver; existing provenance text names Code as producer | No reuse of historical LA-P1-04. Receipt is not verification; tool changes do not automatically create an independent verifier or replace Judge acceptance |
| D-200's tier table says the return path changes no sequence/DoD and omits the affected run/report sources | Record the bounded bootstrap sequence/DoD in the existing Build Spec and the run/report changes in Phase Closure; update docs README and the handoff SOP/template with the agreed semantics. Declare Inventory's exact applicability rather than creating a second tracker | Register, shared rules, Build Spec, run/report contract and channel documentation agree. Product PRD, functional requirements, product UML/data flow and Encyclopedia are unaffected by this development-tool policy |

These are proposed clarifications of T2's implementation, not new rules applied by Lane B. The
proposed ownership exceptions belong in D-200's correction and the byte-identical shared core
before a dependent tool edits those paths. If the original Judge act does not cover a material
extension, present that exact extension once; do not re-ask whether Lane A is one lane.

### Child 2 — make the complete channel recognize A entries

The filename classifier is **`ENTRY_FILE` in `scripts/checks/handoff-fields.mjs:130`**.
`handoff-response.mjs:61` and `closure-readiness.mjs:61` import it. Changing only the former is
the wrong ownership target and can leave closure blind. `channel-docs.mjs:123` separately excludes
only B/C entries from its instruction-document scan.

**Draft implementation contract:** widen the shared classifier to the agreed A/B/C series; make
channel-docs consume the same classifier; update the reader comments. Preserve all current B/C
receipt, phase-scoped closure, report and independence behavior. Use isolated fixtures to prove:

- an unacknowledged A finding is discovered and fails receipt; Cowork's named receipt clears
  that finding without closing it;
- an A Applied entry blocks its owning phase's closure and does not block another phase;
- a valid A report carries its assigned unique Run and filled Evidence; unregistered/duplicate
  runs, closure-only fields and present-but-blank Evidence are rejected; reports are excluded
  from unresolved-defect counts;
- A entry prose is not scanned as channel instructions; actual README/TEMPLATE still are;
- B/C behavior and the agreed verifier restrictions remain intact.

The documented report contract requires Evidence, but the current reader does not reject an
entirely omitted Evidence field. Do not claim that existing control; any enforcement extension
must be explicitly included in the bounded implementation rather than introduced silently.

**Correct the claimed ordering guarantee.** `channel-docs` validates fields, resolutions and
tallies; it does not enforce filename-series declarations. A read-only in-memory counterexample
added an A-series explanatory sentence to its README/TEMPLATE inputs and still returned no
findings. Thus "README/TEMPLATE first necessarily fails" is false.

Use instead: **do not activate/file A entries until readers, documentation, run registration and
fixtures agree.** Prepare both sides of the contract before implementation; finish their owning
tool passes before activation. Do not edit a checker just to force the earlier prediction true.

### Child 3 — implement tool crossing as a separate bounded control

`.githooks/commit-msg:9` delegates to `scripts/lane-gate.mjs`; the latter currently returns early
when only one lane is touched. A Cowork/Code crossing is precisely a one-lane change.

**Draft:** define tool classification once with the existing lane classifier, applying exact-file
exceptions before directory rules. The gate must evaluate lane and tool crossings independently
before deciding that no trailer is needed. Preserve Git trailer parsing and the current stated
degraded behavior. Check single-tool, mixed-tool, simultaneous lane/tool crossing, handoff/workflow
exclusions, the `.agents/rules/graphify.md` exception, and missing/wrong/body-only trailers.
Report the paths involved, not an inferred actor: shared Git identity still cannot prove who
performed an edit. A declaration is not authority or a comprehensive unauthorized-edit detector.

The return-channel and tool-crossing controls are distinct children. Do not imply that receipt
recognition proves tool enforcement, or that a turn report independently verifies either control.

### Child 4 — replace the terminal runbook

| Quoted instruction | Verified problem | Corrected plan |
|---|---|---|
| `node docs/graph-fragments/merge7.js` means all curated fragments | With no argument it defaults to **frag7.json only** (`merge7.js:172`); `--all` audits conflicts rather than merging | Enumerate the required existing fragments in dependency order, pass each explicitly, and verify final parity |
| `graphify update --fill-missing` finishes semantics after that merge | The installed command runs another code rebuild; fill-missing narrows description generation and does not guarantee all assistant batches are answered or ingested | Use the installed documentation-update workflow. Complete and ingest live semantic work; verify final curated content after the last operation. Do not silently discard descriptions with another rebuild |
| `stale:false`, or `check-update` saying current, proves source currency | Metadata names 78cd2b3 while source HEAD is 73226b7 | Require final analyzed HEAD equality, semantic-work inspection and curated parity separately, then the full consistency suite |
| `git gc --prune=now` clears index/HEAD lock litter | Git garbage collection prunes objects; it is not general lock cleanup. Removing the age grace is inappropriate around uncoordinated writers | Remove it from this runbook. Treat an actual blocking lock as a separate maintenance task: identify the exact file and coordinate all writers before a specifically authorized repair |
| Four commits remain credential-blocked | A fresh fetch during this review finds local and upstream both at 73226b7 | The separate push is complete. Recheck only the actual later outgoing range before another authorized push; do not repeat the old push instruction |

Keep ignored runtime metadata untracked. Proposed tracked graph artifacts still require portable
repository-relative paths; existing ignored-runtime scan findings do not automatically block a
documentation packet. Do not delete pending markers as a substitute for ingesting semantic work.

### Parent-first follow-up and closure evidence

| Order | Owner / action | Completion evidence |
|---|---|---|
| 1 | Cowork: correct D-200, surface exceptions, return/run/receipt contract, affected tiers and bounded implementation scope | Exact source packet and applicable authority; no build inferred from ownership |
| 2 | Claude Code: implement the shared A-entry readers and isolated fixtures within that scope | A-positive/A-negative cases and existing B/C behavior pass; no live mutating fixture concurrency |
| 3 | Claude Code: implement the separately specified tool classifier/gate | One canonical classifier, independent lane/tool trailer checks, overlap and negative cases verified |
| 4 | Cowork: finish current-use SOP/template and routing clauses; assign the actual report run | A series activated only when the whole contract is usable; A-001 is a report, not a closure label |
| 5 | Designated Lane A operator: final source/graph synchronization and full local validation | Final committed HEAD, complete claimed semantic scope, preserved curated layer and actual check results; new review entries included |
| 6 | Independent reviewer: review the completed correction and implementation at an immutable revision | This originating entry receives its own supported resolution; no bulk promotion of B-077's children, B-084 or B-071 |

The two implementation children have separate gates. Tool-crossing enforcement is not a
prerequisite for opening the A channel once its readers, documentation, run contract and fixtures
pass. The displayed order is a work sequence, not a new dependency between those controls.

No new artifact is needed to duplicate the backlog. This new B entry owns D-200's distinct
orchestration defect; B-084 owns intake requirements, and the existing reports retain work history.
Remove copied live counts from operative rules/decisions; derive them from the channel when needed.

## What you did instead

Lane B reviewed the sources and prepared this correction plan, without changing governed docs,
shared rules, scripts, hooks, workflows, application code or lane state. No A entry, commit, push,
garbage collection or lock deletion was performed. Lane A has not yet acknowledged this new entry;
its blank response field is intentional and must not be filled with an invented receipt.

**Unintended local runtime write:** during read-only review, a delegated reviewer invoked bare
`merge7.js` expecting usage output. It wrote only ignored `.graphify/graph.json`, upserting the
existing nine nodes and twenty-five edges for frag7, with no additions. Branch metadata was not
changed. This mistake was disclosed immediately; no unverified rollback was attempted. That
operation is not a full rebuild, not synchronization, and not part of the proposed implementation.

**Baseline evidence:** the full local suite at 73226b7 reports 16/17 passing, with docs-drift the
sole failure. `check-update` reports current while the analyzed HEAD remains 78cd2b3: this shows
why the checks must be read together. Before this review the tracked tree was clean; local and
upstream match after fetch. B-085's verification is committed; B-084 remains Applied. Adding this
unacknowledged review entry requires a new post-edit check and later graph/receipt handling; the
baseline result must not be reused for that changed tree.

**Post-edit validation:** 14/17 checks pass. The three findings are the existing HEAD/graph drift,
this new B-086 missing from the graph, and its genuinely unacknowledged `Lane A` field. The
remaining checks pass, including B-084's updated nonterminal evidence anchor. All 17 local file
links across the two edited/created handoffs resolve; the diff has no whitespace errors. These
review records remain uncommitted. Cowork must acknowledge the new entry; the designated graph
operator must include the final committed review in synchronization. No receipt was fabricated
and no check was weakened to obtain a green result.

| Verdict | Tier / item | Follow-up phase |
|---|---|---|
| Approve | One Lane A, T2 direction, existing-channel return concept | Phase 1: preserve the decision without calling implementation complete |
| Approve-with-conditions | D-200 Register/shared-core/reporting packet | Phase 1 Cowork: complete ownership, activation contract and propagation |
| Defer | A-channel readers, tool gate and full D-200 verification | Phase 1 Claude Code implementation, then independent review; no build in this task |
| Reject | Filter-only completion, claimed README-order failure, incomplete graph restore and garbage collection as lock cleanup | Phase 1: replace the quoted runbook and use the explicit success criteria |
