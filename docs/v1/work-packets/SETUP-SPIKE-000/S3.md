# Work packet — `SETUP-SPIKE-000` / `S3` — Board, filters, audit log

**Created:** 2026-09-17 (`D-240`, `docs/handoff/B-118`). **Owner:** Lane B (build), Lane A (tracking).
**Status:** `Not started`.

**This file is the MMF checklist only.** Sequence and scope narrative stay in `V1-BUILD-SPEC.md` §4
"S3 — Board, filters, audit log" — the one owner of that fact.

## Requirement / decision anchors

`FR-08`, `CR-13`, `D-12` (intent codes queryable).

## Rank

Sequenced after `S2` in `V1-BUILD-SPEC.md`'s scaffold sequence — not independently ranked.

## Dependencies

`S2`'s `T6→T5` return-rate logging must exist before this sprint's audit view can expose it.

## DoR checklist

- [ ] `S2` (or at minimum its return-rate logging) reaches a buildable state
- [ ] Filter/board UI scope confirmed against `FR-08`/`CR-13`'s current text (not drafted here)

## DoD checklist

Filterable board (`FR-08`, `CR-13`) built and working · audit view exposing the `T6→T5` return rate ·
intent codes queryable (`D-12`).

## Owner / lane

Lane B builds; Lane A tracks.

## Artifact paths

Not yet created.

## Test / evidence links

None yet — no code exists for this sprint.

## Transfer disposition (`D-244`, 2026-09-20 — transfer, not completion)

| Behaviour | Destination |
|---|---|
| Board/filter visibility (`FR-08`, `CR-13`, `AC-13`) | `V1-SM06` |
| `T6`→`T5` return-rate audit view | **Held** with the `T6`-dependent scope; not in `V1-SM06` while `T6` is outside V1 |

This packet is **not retired and earns no DoD**. **Transfer mapped; closure pending** (`D-248`): the setup programme stays open while this packet or its residuals are non-terminal. The dependency line above on `S2`'s return-rate logging
applies only to the held item.

## Terminal disposition

Open. Not started.
