# Attempt record — `SV-001` (retrospective wrapper)

**Created:** 2026-09-25 (`D-264`). **Owner:** Lane A. **Parent:** `SETUP-SPIKE-000`.

| Field | Value |
|---|---|
| `Retrospective-Record` | `true` |
| `Template-ID` / `Template-Version` | `legacy/embedded`. This attempt predates `dor-dod-validation/v1` and is **not** claimed to have run under it |
| `Attempt` | `SV-001` |
| `Previous-Attempt` | none |
| `Success-Baseline` | none; **this attempt is the historical baseline** for later setup-validation attempts |
| `Trigger` | `V1-SM05` pre-selection readiness (`D-245`, `D-254`) |
| `Status` | **Complete (historical).** It is not reopened, re-checked or copied |

## What this attempt was

The **pre-selection Lane A handoff-validation** of `V1-SM05`: rows `DOR-R1`–`DOR-R7` in
`docs/v1/work-packets/V1/V1-SM05.md`, with their evidence rows and the Jev readiness receipt
`docs/v1/work-packets/V1/receipts/V1-SM05-jev-readiness.json` (pass at clean commit `719e72b`, `D-261`). It tested that the
governed requirements, behavioural scenarios and readiness script agreed with each other before selection.

**It was not a feature DoR/DoD run.** No feature behaviour was executed against application code, and it is not an
acceptance test. The rows stay where they are recorded; this file points to them and does not restate them.

## Outcome and later-observed limitations

| Item | Value |
|---|---|
| `Outcome-At-Time` | **Complete.** All seven rows were checked on the evidence required then (`D-256`, `D-259`–`D-261`), Issue #1 was created on the normal path (`D-262`), and the State-1 record was repaired (`D-263`) |
| Gate claim | Pre-selection handoff validation complete; State-1 objects may be created |
| Supporting evidence | The seven evidence rows in `V1-SM05.md`; the Jev receipt; `B-131`, `B-133`, `B-134`, `B-135` |
| `Later-Observed-Limitations` | At the selection check (2026-09-25, `D-264`), the Judge found that setup gaps had not been transferred into this attempt: `D-258` closed the setup root from terminal child status without item-level transfer receipts. The warranted loader spike (`D-257`, `B-130`), the code-navigation question and the open `B-071`/`B-095`/`B-104` contract children were all outside its coverage |

Those limitations are carried into `SV-002` (`SV-002.md` §4). They do not change this attempt's
completed-at-the-time outcome.
