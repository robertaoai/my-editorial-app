# Attempt record — `SV-002` (setup validation)

**Created:** 2026-09-25 (`D-264`). **Owner:** Lane A. **Parent:** `SETUP-SPIKE-000`. Raised through `docs/handoff/B-136`.

## 1. Identity

| Field | Value |
|---|---|
| `Template-ID` | `dor-dod-validation` |
| `Template-Version` | `v1` (`docs/templates/dor-dod-validation/v1.md`) |
| `Template-SHA-256` | `ca605cd6d477ad9d3e9919569fe655d1ba98df3ee8c86dfe57945aec0587be90` |
| `Attempt` | `SV-002` |
| `Previous-Attempt` | `SV-001` — its rows and evidence stay where they are recorded |
| `Success-Baseline` | `SV-001` — outcomes are compared without changing its completed-at-the-time status |
| `Trigger` | Judge pre-selection validation finding, 2026-09-25 (`D-264`) |
| `Blocks` | `V1-SM05` selection, its State-2 work order and Lane B activation |
| `Status` | **`DoR open`** |

**Historical-input rule.** `SV-001` is cited and never called a feature DoR/DoD. Its rows are not copied, reopened or
re-checked.

## 2. Coverage index — one row per scope (template §2)

| Scope | Included (this attempt) | Excluded, with reason |
|---|---|---|
| **Project** | `D-248`, `D-257`, `D-258`, `D-264`; Build Spec §1; Artifact Inventory | Frozen PRD/Charter: no constraint changes |
| **Product** | `Modular_PRD` §8/§8.1; `FR-15`, `AC-23`–`AC-26`, `SM05-N1`–`SM05-X1` (for `SV2-U04`); MMFs in V1 scope: `V1-SM05` (`BLOCKED`, not selected), `V1-SM06` (not selected; depends on `V1-SM05` DoD) | Fn_Specs/SPECS behaviour: unchanged unless `SV2-U04` proves a contract change |
| **Execution scope** | `SETUP-SPIKE-000` container; units `SV2-U01`–`SV2-U04` below | Application code, migrations, workflows: no construction is authorized |
| **Backlog refinement** | `B-130` (returned by `D-264`); `B-071`, `B-095`, `B-104` (`SV2-U04`); `B-136` (docket); `B-120`/`B-125` (State-1 progress only) | Other Open entries: no intersection shown with this gate; each keeps its own owner, with no bulk change |
| **Historical dialogue and tooling** | C-002 loader draft (advisory, via `B-130`); the Lane C `ripwire` proposal (via `B-136`); the Jev readiness receipt (`SV-001`) | Conversation prose is provenance only and is not copied |
| **Attempt lineage** | `SV-001` | — |

## 3. Units

| ID | Unit | Owner | Acceptance proof | Return condition |
|---|---|---|---|---|
| **SV2-U01** | Record `D-264`: continue the setup root, return `B-130`, mark `V1-SM05` `BLOCKED`, create the template and `SV-001`/`SV-002` | Lane A | One governed commit propagated under `D-54`; the consistency suite passes; the graph is current | An independent reviewer confirms that only the intended status changes were made, with no DoR/DoD, lane, construction or deployment authority changed |
| **SV2-U02** | Loader characterization — which rule/skill files each agent loads, any limit and its source, and exactly what text is visible | Lane A (protocol, acceptance docket); Lane B and Lane C execute their own runs (see §3.1) | Per run: tool, version and backend; loaded-file inventory; start/middle/end sentinels; byte and character observations; truncation or error evidence; reproducible steps. Each run distinguishes discovery failure, per-file truncation, total-context truncation and complete loading | The Judge accepts the combined report and records either "no remediation" or a separately bounded remediation, which is then applied and independently verified. Any activated lane has returned the lock to Lane A |
| **SV2-U03** | Code-navigation need, with `ripwire` evaluated as a candidate | Lane A (evaluation); Lane B (consumer evidence) | `SV2-U03-code-navigation-evaluation.md` (drafted 2026-09-25, read-only, nothing installed) plus Lane B's consumer review | The Judge records one outcome: provision the tool, use an existing path with evidence, or waive |
| **SV2-U04** | Whether the unresolved children of `B-071`, `B-095` and `B-104` are consumed by the bounded `V1-SM05` slice | Lane A (mapping); Lane B (feasibility review) | A row-level matrix from each child to `FR-15`, `AC-23`–`AC-26`, `SM05-N1`–`SM05-X1`, data writes and the first work-order child; each row says blocking or non-blocking and cites its controlling Register clause | Every blocking row is decided, propagated, applied and independently verified; non-blocking rows keep their owner and return condition |

### 3.1 `SV2-U02` run tracking — the Lane B tracker is the parent of the Lane C tracker (Judge, `D-264`)

| Tracker | Scope | Run IDs |
|---|---|---|
| **`SV2-U02-B`** (parent) | Lane B's agent loader: Codex reading `AGENTS.md` and its rule and skill files | `SV2-U02-B-R1`, `-R2`, … |
| **`SV2-U02-C`** (child) | Lane C's agent loader: Antigravity reading `.agents/rules/` and its skill files | `SV2-U02-C-R1`, `-R2`, … — each run is filed **under** a named `SV2-U02-B-R*` run |

- **If Lane B reruns:** open `SV2-U02-B-R(n+1)`. It tracks Lane B **and** a fresh Lane C run beneath it.
- **If Lane B's run passed and only Lane C gaps appear later:** open `SV2-U02-C-R(m+1)` under the passed Lane B run.
  The Lane B run is not reopened.
- A run is executed only in that lane's own selected turn, or from authenticated evidence the Judge supplies. Lane C is
  `Blocked` today, so no Lane C run can start yet.

| Run | Parent run | State | Evidence |
|---|---|---|---|
| `SV2-U02-B-R1` | — | Not started | — |
| `SV2-U02-C-R1` | `SV2-U02-B-R1` | Not started | — |

## 4. Drift ledger — `SV-001` → `SV-002`

| Item | Prior attempt evidence | Drift class | Why the gate missed it | Current unit and owner | Acceptance proof | Return condition |
|---|---|---|---|---|---|---|
| Setup closure rule | `D-258` item 2 closed the root from terminal child status | `Changed` | The closure rule required no item-level transfer receipt | `SV2-U01`, Lane A | `D-264` propagation plus independent review | As `SV2-U01` |
| Loader characterization | `D-257` items 1–3 (warranted, uncommissioned); `B-130` Deferred | `Previously omitted` | The spike had a return trigger but sat outside both the setup closure and the pre-selection gate | `SV2-U02`, Lane A with Lane B/C runs | As `SV2-U02` | As `SV2-U02` |
| Code-navigation tool | Absent | `New` | Lane C proposed a remedy before the consumer need was proven | `SV2-U03`, Lane A with Lane B | As `SV2-U03` | As `SV2-U03` |
| `B-071`/`B-095`/`B-104` contract children | Absent from the `DOR-R*` evidence; the entries remained Open | `Previously omitted` | Selection readiness never tested whether the bounded slice consumes them | `SV2-U04`, Lane A with Lane B | As `SV2-U04` | As `SV2-U04` |

## 5. Success-drift comparison against `SV-001` (template §6)

| Dimension | `SV-001` baseline | `SV-002` success criterion | Classification |
|---|---|---|---|
| Scope coverage | `DOR-R1`–`DOR-R7` plus the Jev sources only | All six coverage scopes indexed with exclusions (§2) | *at DoD* |
| Transfer completeness | Terminal children, no item-level receipt | Every unresolved item has a disposition or destination, owner, proof, return condition and receipt | *at DoD* |
| Evidence strength | Evidence accepted for the earlier gate | Every `SV2-U*` result is reproducible, reviewed where required and pinned to a revision | *at DoD* |
| Blocker disposition | Later blockers were not visible | `SV2-U01`–`SV2-U04` each meets its return condition | *at DoD* |
| Gate result | Validation appeared complete; State-1 objects were created | Setup closure and SM05 selection are refused while anything required is unresolved | *at DoD* |
| Discovery timing | Gaps surfaced at the selection check | Equivalent gaps surface inside `SV-002`, before any lift or selection | *at DoD* |

## 6. DoR

| ID | Ready condition | Evidence required before execution | State |
|---|---|---|---|
| `SV2-DOR-01` | Authority and scope fixed | Judge ruling; exact `D-257`/`D-258` supersession table; exclusions (`D-264`) | [ ] |
| `SV2-DOR-02` | Coverage, item drift and success baseline accounted for | §2, §4 and §5 complete | [ ] |
| `SV2-DOR-03` | Units bounded | §3 names each unit's owner, inputs, output, proof, stop and return condition | [ ] |
| `SV2-DOR-04` | Execution access available | Repository state; an authorized Lane B and Lane C measurement turn or Judge-supplied evidence; no secret or hosted mutation needed | [ ] |
| `SV2-DOR-05` | Evaluation methods fixed | Loader sentinel protocol, code-navigation criteria and mapping columns written before any results | [ ] |
| `SV2-DOR-06` | Governance boundary fixed | Readiness alone authorizes no construction, feature tests, migration, deployment, root `GEMINI.md`, skill rewrite, dependency install or bulk handoff closure | [ ] |

## 7. DoD

| ID | Done condition | Completion evidence | State |
|---|---|---|---|
| `SV2-DOD-01` | Governance applied | `SV2-U01` committed, checks pass, graph current, independent review | [ ] |
| `SV2-DOD-02` | Coverage, item drift and success drift closed | Every scope dispositioned; every ledger row proven or retained with owner, receipt and return; every dimension classified | [ ] |
| `SV2-DOD-03` | Loader question resolved | `SV2-U02` runs complete per §3.1 and Judge-accepted; any remediation separately decided, applied and verified | [ ] |
| `SV2-DOD-04` | Code navigation resolved | `SV2-U03` outcome recorded and proven | [ ] |
| `SV2-DOD-05` | Contract dependency resolved | `SV2-U04` matrix; blocking rows decided, applied and verified | [ ] |
| `SV2-DOD-06` | Attempt accepted | Evidence index links `DOD-01`–`05` to exact revisions; the Judge accepts `SV-002` and separately decides whether to lift the SM05 block | [ ] |

| DoR row | DoD proof |
|---|---|
| `SV2-DOR-01` | `SV2-DOD-01`, `SV2-DOD-06` |
| `SV2-DOR-02` | `SV2-DOD-02`, `SV2-DOD-06` |
| `SV2-DOR-03` | `SV2-DOD-01`, `SV2-DOD-03`–`SV2-DOD-05` |
| `SV2-DOR-04` | `SV2-DOD-03`, `SV2-DOD-04` |
| `SV2-DOR-05` | `SV2-DOD-03`–`SV2-DOD-05` |
| `SV2-DOR-06` | `SV2-DOD-01`, `SV2-DOD-06` |

A materially new setup gap found after this attempt closes opens `SV-003`, with a drift ledger from `SV-002`.
