# Work packet — `SETUP-SPIKE-000` / `S4` — Publication and fallback

**Created:** 2026-09-17 (`D-240`, `docs/handoff/B-118`). **Owner:** Lane B (build), Lane A (tracking).
**Status:** `Not started` — two design items open before build per `V1-BUILD-SPEC.md` §4.

**This file is the MMF checklist only.** Sequence and scope narrative stay in `V1-BUILD-SPEC.md` §4
"S4 — Publication and fallback" — the one owner of that fact.

## Requirement / decision anchors

`FR-09` (WordPress automation — recommended first channel is LinkedIn `ManualReady`, no automation,
per `G9`), `NFR-05` (bounded retry), `NFR-07`/`SEC-02` (credentials server-side only), `TC1`
(privileged-write-path amendment), `D-143` (Supabase Edge Function is the publish path, `pg_cron` is
the retry scheduler — `docs/specs/SPECS-PUBLICATION.md`).

## Rank

Sequenced after `S3` in `V1-BUILD-SPEC.md`'s scaffold sequence — not independently ranked.

## Dependencies

Credential isolation design (`NFR-07`/`SEC-02`) and the `TC1` privileged-write-path amendment —
`D-143` ruled the runtime, not these two; both remain open before this sprint builds.

## DoR checklist

- [ ] Credential isolation design settled (`NFR-07`/`SEC-02`)
- [ ] `TC1` privileged-write-path amendment settled
- [ ] First-channel confirmed as LinkedIn `ManualReady` (recommended, `G9`) before specifying WordPress automation

## DoD checklist

`publication_targets`/`publications` tables in use · WordPress publish or LinkedIn `ManualReady`
working · `T10` auto-fallback · `T11` Chief Editor manual confirm · bounded retry (`NFR-05`) ·
credentials server-side only (`NFR-07`/`SEC-02`) · first genuine use of
`SUPABASE_SERVICE_ROLE_KEY`, the architecture's first privileged path (`TC1`).

## Owner / lane

Lane B builds; Lane A tracks.

## Artifact paths

Not yet created.

## Test / evidence links

None yet — no code exists for this sprint.

## Transfer disposition (`D-243`, `D-244`, 2026-09-20 — transfer, not completion)

| Behaviour | Destination |
|---|---|
| LinkedIn target-level `ManualReady` | `V1-SM06`, per `D-243` — a `publication_event_type`, not an article state |
| Automated WordPress, retry (`NFR-05`), privileged credentials (`NFR-07`/`SEC-02`/`TC1`), scheduler | Future **V2-target backlog**; no `V2` opened, no `[V1→V2]` marker |
| `T11`/`FR-10` live-URL confirmation and `Published` promotion | Future **V2-target backlog**; `[V1]` origin retained |

V1 `ManualReady` needs no external-channel credential; the two design items in this packet's DoR bind the
future automated path only. This packet is **not retired until every remaining behaviour and citation has
moved**, and earns no DoD. **Transfer mapped; closure pending** (`D-248`): the setup programme stays open while this packet or its residuals are non-terminal.

## Terminal disposition

Open. Not started.
