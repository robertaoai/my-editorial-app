# Work packet — `SETUP-SPIKE-000` / `S2` — Line assignment and four-eyes

**Created:** 2026-09-17 (`D-240`, `docs/handoff/B-118`). **Owner:** Lane B (build), Lane A (tracking).
**Status:** `HELD` — `D-171` explicitly holds this sprint pending a fresh build authorization; not
`Not started` in the generic sense, `HELD` for a named reason.
**Terminal, 2026-09-24 (`D-258`): `Deferred — closed without DoD credit`.** See Terminal disposition below.

**This file is the MMF checklist only.** Sequence, full scope narrative, and every cited decision's
rationale stay in `V1-BUILD-SPEC.md` §4 "S2 — Line assignment and four-eyes" — **the one owner of
that fact**. This packet does not restate it; it tracks readiness and evidence against it.

## Requirement / decision anchors

`OD1`/`OD2` (gates production done, not Phase 0 done — a negative `OD2` voids this sprint's model),
`SEC-01` (hard block on one identity holding both Line 1 and Line 2), `D-163`–`D-165`, `D-171`,
`D-175`, `D-177`, `D-178`, `D-180`, `D-181`.

## Rank

Not ranked against other packets yet — `S2 Unit 1` (T5 human-only trigger verification + minimal
RPC surface) is the only authorized subset (`D-164`/`D-165`); `S2 Unit 2` (T6, blind first pass,
`T6→T5` return-rate log, `EmergencyBypass`) is **not yet authorized**.

## Dependencies

`F6` (target Line/executor matrix rewrite) must land before either unit's DoD in `V1-BUILD-SPEC.md`
§4 is buildable as currently held. `S2 Unit 1` alone does not require `F6`.

## DoR checklist

- [ ] Fresh build authorization selecting one `T5`/`T6` contract order (target vs. current — `D-171`'s
      hold names this as the precondition)
- [x] `S2 Unit 1` scope specified — `LANE-B-WORK-ORDER.md` §2.2d
- [ ] `S2 Unit 2` scope authorized (not yet — `D-165`)

## DoD checklist

See `V1-BUILD-SPEC.md` §4's target DoD list (`D-178`) — cited, not restated. Neither unit alone
earns M2 or production-done.

## Owner / lane

Lane B builds; Lane A tracks and does not cross into `app/`, `lib/`, `components/`, `supabase/`.

## Artifact paths

Not yet created — `S2 Unit 1`'s minimal surface targets a security-invoker Postgres function called
via `supabase.rpc()`, per `LANE-B-WORK-ORDER.md` §2.2d.

## Test / evidence links

None yet for this sprint's own code — no code exists for it.

**Setup-baseline provenance (`D-255`, 2026-09-23).** The local and Supabase A01 receipts under
`docs/handoff/artifacts/B-119/`, with their five operator facts, are a one-time independent baseline
recorded at this checkpoint. Their evidentiary scope is **non-transferable**: the `D-244` transfer below
moves behaviour, not this evidence, and the baseline gates no `V1` packet. It earns this packet no DoD.

## Transfer disposition (`D-244`, 2026-09-20 — transfer, not completion)

| Behaviour | Destination |
|---|---|
| Line/RACI and route-context display the core journey needs | `V1-SM05` — display/provenance only, anchored to `FR-04`/`FR-05`, `SEC-01` and the applicable RACI source |
| Security-invoker RPC, `T6`, blind pass, `EmergencyBypass`, return evidence | **Stays `HELD`** (`D-171`); not attached to `V1-SM05`, `V1-SM06` or `V2` |

This packet is **not retired and earns no DoD**. **Transfer mapped; closure pending** (`D-248`): the setup programme stays open while this packet or its residuals are non-terminal. Its own DoR/DoD lists above are unchanged.

## Terminal disposition

Open. `HELD` per `D-171` until a fresh authorization lands.

**Closed 2026-09-24 (`D-258`), appended — the line above is history.** `Deferred — closed without DoD
credit`. Transferred behaviour is owned by its `D-244` destination and earns this packet nothing; the held security-invoker RPC, `T6`, blind pass, `EmergencyBypass` and return evidence stay `HELD` under `D-171`. The setup root is closed, and this packet does not reopen: a residual returns only through its own owner. The file is kept as history, not retired.
