# Work packet — `V1` / `V1-SM05` (`MMF-V1-CORE`)

**Created:** 2026-09-17 (`D-240`, `docs/handoff/B-118`). **Owner:** Lane B (build), Lane A (tracking).
**Status:** Not started; not selected (`D-232`). **Current, 2026-09-25 (`D-264`): `BLOCKED — SETUP-SPIKE-000 validation
attempt SV-002 open; not selected; feature DoR/DoD not yet run; construction not authorized.`** This is a packet status,
not a lane state. The checked `DOR-R1`–`DOR-R7` rows below are the completed **pre-selection handoff validation**,
recorded as attempt `SV-001` (`../../SETUP-SPIKE-000/SV-001.md`); they are not an executed feature DoR. The block is
tracked in `SV-002` (`../../SETUP-SPIKE-000/SV-002.md`). The first feature run, `V1-SM05-FV-001`, is created after
selection and runs only once Lane B has written behavioural test code.

**Durable-owner receipt, 2026-09-26 (`D-269`, Judge Option A).** This packet receives, as their long-term owner, the
obligations absorbed from `docs/handoff/B-120` Parent 5 and `docs/handoff/B-125` Parent 6: the Judge's selection of
`V1-SM05`, the `D-242` State-2 work order, Lane B activation, and construction evidenced by `V1-SM05-FV-001`. Both
source entries are `Superseded`, terminal, with no return trigger; `docs/handoff/B-136` Parent 3 holds the transition
receipt. **Acceptance proof:** the Judge's recorded selection and work order, then `V1-SM05-FV-001`'s feature DoD and
the Judge's acceptance. **Gate condition:** only after `SV-002` is accepted and the block is lifted. Nothing here is
complete; the status above is unchanged.

**This file is the MMF checklist only.** Scope narrative, DoR consolidation and cross-references stay
in `V1-BUILD-SPEC.md` §1's `DOR-R1`–`DOR-R6` table and `requirements-traceability-map.md` §6.1/§6.3/
§6.4 — the owners of those facts. This packet tracks readiness and evidence against them, not a
restatement.

## Requirement / decision anchors

First working-software increment — a **limited Route-1 evidence slice** (`D-249`), fixed to `ROUTE-PROD-1`
(`D-232`): it records and displays accepted business-stage, role, task and evidence facts and **does not claim
`transition:T*` execution**. It satisfies the customer's `CR-19` scenario **only partially** (`D-247`); the five-gate
execution and Chief Journalist approval it names are neither admitted here nor assigned a destination. **Product owner (`D-261`): `US-15`/`FR-15`, `AC-23`–`AC-26`** (`Modular_PRD.md` §4/§5/§9.1), elaborated by `FN-GATES-01-05.md`
§4.3/§4.4; `CR-09` (intake) and `CR-19` (partial) as direct customer anchors; `AC-02` (surface duplicate refusal, `FR-01`); `D-233` (`OP-PITCH`/
`OP-DRAFT`/`OP-FINAL-SIGNOFF` contracts), `D-236`–`D-239` (source-RACI corrections, role-identity
split, ranking/UI-assistance boundary).

**Option A boundary (`D-260`, 2026-09-24, Judge).** This packet is the evidence slice of `business:T1`–`T5`
stage, task and evidence records. Technical `transition:T5` review bundles and `transition:T6` approval gates —
`FR-04a`/`FR-05a`, `AC-05a`/`AC-06a` — are **not** in it: they stay held under `D-171`, unchanged in
`Modular_PRD.md` §9, and no later packet is named for them. `AC-11`/`AC-12` (transition-row audit) are likewise
outside it; this packet's append-only history is proven by `SM05-RV1`/`SM05-RV2`.

**Added by `D-244` (2026-09-20) — display anchors.** The non-enforcing Line/RACI and route-context display
moves here from `SETUP-SPIKE-000/S2`, citing `FR-04` (accountable review) and `FR-05` (independence
classification) **as display provenance only, not implementation acceptance (`D-261`)**, with `SEC-01` and the applicable RACI source. `D-239` still excludes enforcement, and the
held `S2` RPC/`T6` scope is **not** imported. Encyclopedia review set: Entries 01, 05 and 06.

## Rank

`V1-SM05` precedes `V1-SM06` — the evidence slice before board visibility and the bounded `ManualReady` event.

## Branch, PR and migration boundary (`D-252`, 2026-09-22)

The `V1-SM05` branch and pull request are **stacked**: based on and targeting
`docs/journal-2026-08-16`, not `main`; baseline promotion to `main` is a separate, later act,
pending acceptance of this packet's DoD. **"Migration" means application to the persistent hosted
Supabase environment**; the local-PostgreSQL run used for mechanical verification is a disposable
replay, not a migration. `supabase/migrations/0002_s1_editorial_schema.sql` is an existing
setup-prepared candidate, allocated to this packet — its prior setup status earns no `V1`
feature-construction credit. Construction under this packet is **local-only**; hosted `0002`
migration and Supabase A02 follow only after this packet's DoD is accepted **and** a separate
baseline-promotion PR is accepted. The readiness pull request is **two-state**: State 1 presents
readiness only — Judge selection, the work order and Lane B activation precede State 2
(implementation). Local and Supabase A01 receipts are `SETUP-SPIKE-000/S2` baseline evidence only
(`D-255`, 2026-09-23): they are not readiness evidence for this packet and do not satisfy final `D-251`
production classification, which stays open for a post-promotion A02. Push authorization and the
stacked-integration method are **not** decided by this note.

## Issue / branch / PR traceability (`D-253`, 2026-09-22)

- **Branch, approved:** `features/feature-V1-SM05`, created by native Git branching from the
  verified pushed remote tip of `docs/journal-2026-08-16` (no ZIP-packaged initialization).
- **GitHub Issue:** created from completed DoR evidence (`R2`–`R6`, an accepted `R6` deferral
  counting as complete; **`R7` added 2026-09-24, `D-259`** — the Issue waits on it too) from
  `.github/ISSUE_TEMPLATE/v1-mmf.md` (`D-259`) — Issue number/URL recorded here once it exists; external tracking, no
  repository file (`V1-ARTIFACT-INVENTORY.md`, `D-253`). **Recorded 2026-09-25 (`D-262`): Issue #1,
  `https://github.com/robertaoai/my-editorial-app/issues/1`**, created after `DOR-R1`–`DOR-R7` were all
  checked — the normal `D-254` path, no remediation.
- **Branch created (`D-262`, 2026-09-25):** native Git branch at `9b2ddf3`, equal to the pushed
  `origin/docs/journal-2026-08-16` tip and pushed as `origin/features/feature-V1-SM05`.
- **State-1 PR:** documentation-only; DoD gates acceptance and merge. **Opened 2026-09-25 as draft
  PR #2, `https://github.com/robertaoai/my-editorial-app/pull/2`** (`D-262`, Judge authorization under
  `D-253`), base `docs/journal-2026-08-16`, head `features/feature-V1-SM05`, linked to Issue #1. Stays
  State 1 until Judge selection, the `D-242` work order and Lane B activation.

## Dependencies

`DOR-R1`–`DOR-R7` (`V1-BUILD-SPEC.md` §1; `DOR-R7` added by `D-259`, 2026-09-24) must each reach their
completion evidence before this packet is buildable. `DOR-R2`/`DOR-R4` are resolved as application-selection rules, and their failure/replay
detail is drafted at `FN-GATES-01-05.md` §4.3 (`D-249`); `DOR-R5` and `DOR-R6` were checked 2026-09-24 (`D-259`) on the
review evidence recorded below; `DOR-R7` was checked by `D-260`, reopened by `D-261` and re-checked on a receipt passing Product-intent parity at `719e72b`.
**Every DoR row is checked.**

## DoR checklist

**Gate, stated explicitly (`D-254`, 2026-09-22).** Every row below must be checked or explicitly
Judge-deferred before this packet's GitHub Issue and feature branch carry any readiness, selection,
work-order, lane-transfer or construction authority. An Issue created before that point is remediated
by updating it, never by treating its existence as readiness (`D-254`). No `V1-SM05` Issue exists as of
2026-09-23 (`docs/handoff/B-126`), so the normal post-DoR creation path applies. **Followed 2026-09-25
(`D-262`):** Issue #1 was created after every row below was checked.

**Scope, stated explicitly (`D-255`, 2026-09-23; amended `D-259`, 2026-09-24).** This packet's readiness is exactly `DOR-R1`–`DOR-R7`;
`D-259` added `DOR-R7` by Judge ruling.
The former identity-compatibility row was removed — A01 is `SETUP-SPIKE-000/S2` baseline evidence only —
and no replacement identity gate is substituted.

**Checkbox meaning, restated 2026-09-22 (`docs/handoff/B-121` Parent 1).** A box is checked only once
Lane B's feasibility review has **accepted** the drafted contract — a drafted-but-unreviewed contract
is evidence available for review, not evidence the review has passed. `DOR-R2`–`DOR-R4` were unchecked
pending that review; Lane B's feasibility acceptance (`docs/handoff/B-123`, 2026-09-22) closed the
review for all three, so they are checked below.

- [x] `DOR-R1` — pre-`T1` record and event contract (schema-neutral contract named)
- [x] `DOR-R2` — `OP-PITCH` atomic contract (rule resolved; failure/replay/idempotency drafted
      2026-09-21 at `FN-GATES-01-05.md` §4.3, `D-249`, identity terms added 2026-09-22; Lane B
      feasibility accepted `docs/handoff/B-123`)
- [x] `DOR-R3` — `EW` refusal/retry contract (drafted at `FN-GATES-01-05.md` §4.3, identity terms
      added 2026-09-22; Lane B feasibility accepted `docs/handoff/B-123`)
- [x] `DOR-R4` — required Route-1 operation contracts (`OP-DRAFT`/`OP-FINAL-SIGNOFF`/`OP-COPY-EDIT`
      resolved; `OP-DRAFT` executor/repeat-dispatch behaviour drafted at `FN-GATES-01-05.md` §4.3 —
      **`OP-COPY-EDIT` and `OP-FINAL-SIGNOFF` execution are explicitly deferred, contract-review only**
      (`docs/handoff/B-121` Parent 3); Lane B feasibility accepted the bounded contract-review scope,
      `docs/handoff/B-123`)
- [x] `DOR-R5` — UI and target visual contract (storyboard Panel A11 authored 2026-09-24, `D-256`; Chief
      Editor walkthrough `W1`–`W3` Accept and Lane B/C review accepted, recorded in Panel A11.5, `D-259`)
- [x] `DOR-R6` — Encyclopedia review (Entries 01/05/06 compared and republished as v15, `D-256`; Lane B read
      the v15 export directly under the Judge's Option 2, `docs/handoff/B-131`, `D-259`)
- [x] `DOR-R7` — Jev System One readiness receipt passes for this packet (`D-259`; checked by `D-260` at `9be84bb`, reopened
      and **re-checked by `D-261`** on a receipt passing scope and Product-intent parity at clean commit `719e72b`; manifest
      `scripts/jev/manifests/V1-SM05.json`, receipt `docs/v1/work-packets/V1/receipts/V1-SM05-jev-readiness.json`)

**DoR evidence status (`D-245`, 2026-09-20).** DoR is **defined, not complete**: a box is checked only where
current evidence supports it, and an unchecked box names what it still needs. **Current fact, 2026-09-25 (`D-263`) — the
sentence above is history:** DoR is **complete**. `DOR-R1`–`DOR-R7` are all checked (`D-259`–`D-261`), and Issue #1
was created after that (`D-262`). Selection, the work order and construction have not started.

| Item | Current evidence | What it still needs |
|---|---|---|
| `DOR-R1` | Checked above; contract named in `V1-BUILD-SPEC.md` §1 and `FN-GATES-01-05.md` §3.0 | — |
| `DOR-R2` | Executor rule resolved (`D-233`, crosswalk §4.1); failure/replay contract and replay/attempt identity terms at `FN-GATES-01-05.md` §4.3 (`D-249`); Lane B feasibility accepted `docs/handoff/B-123` | — |
| `DOR-R3` | Refusal/retry scenarios and legitimate-new-work identity terms at `FN-GATES-01-05.md` §4.3; Lane B feasibility accepted `docs/handoff/B-123` | — |
| `DOR-R4` | `OP-DRAFT`/`OP-COPY-EDIT` resolved; `OP-FINAL-SIGNOFF` accepted (`D-233`, `D-236`–`D-238`); `OP-DRAFT` failure/replay at §4.3 — `OP-COPY-EDIT`/`OP-FINAL-SIGNOFF` execution explicitly deferred, contract-review boundary only (`docs/handoff/B-121` Parent 3); Lane B feasibility accepted the bounded scope `docs/handoff/B-123` | — |
| `DOR-R5` | Storyboard Panel A11 (`D-256`). **Accepted 2026-09-24 (`D-259`):** Chief Editor `W1`/`W2`/`W3` Accept on both walks; Lane B feasibility `docs/handoff/B-131` Parent 1; Lane C review C-003 (user-supplied); all recorded in Panel A11.5, with the `W2` downstream-evidence rule written into Panel A11.2 | — |
| `DOR-R6` | Compared and republished as v15 (`D-256`). **Confirmed 2026-09-24 (`D-259`, Option 2):** Lane B read the v15 export directly (`docs/handoff/B-131`); export stored at `docs/handoff/artifacts/B-131/System_Encyclopedia_V15.txt`, SHA-256 `bc97bebac0f3ac2d3f3d8fed32c3a9cb93684b60f829fee3604b08faf34ace9f`. Entry 04 stays deferred to `V1-SM06` | — |
| `DOR-R7` | **Added (`D-259`); closed (`D-260`, receipt at `9be84bb`); reopened (`D-261`).** `B-134` showed the `D-260` receipt proved agreement among the lower tiers only: the §4.4 scenarios had no `Modular_PRD` owner and `AC-01` carried two meanings. `D-261` adds `FR-15`/`AC-23`–`AC-26`, anchors every §4.3/§4.4 row to them, drops `AC-01`, and adds Product-intent parity to Jev | — **Met:** receipt `pass`, evaluated at clean commit `719e72b` (`D-261` Commit A) |

### DoR → DoD map (`D-259`, 2026-09-24)

Each DoR row names the existing DoD obligation(s) that prove it and the evidence class fixed before the
Issue. **Recorded once here; it adds no DoD row.** Lane B still chooses the exact xDD method per child
(`D-242`). Jev readiness mode reads this table (`DOR-R7`).

| DoR row | DoD obligation | Evidence class |
|---|---|---|
| `DOR-R1` | Behaviour, by namespace; Acceptance cases pass against the real database; Persistence | Database integration and contract evidence |
| `DOR-R2` | Behaviour, by namespace; Refusal and replay | Atomicity, replay and idempotency evidence |
| `DOR-R3` | Refusal and replay; Exclusions verified absent | Negative/refusal and legitimate-retry evidence |
| `DOR-R4` | Behaviour, by namespace; Refusal and replay; Exclusions verified absent | Contract/integration evidence for admitted operations; absence evidence for deferred execution |
| `DOR-R5` | Display, not enforcement; Accepted-contract traceability; Persistence | UI walkthrough and record-to-visible-fact traceability |
| `DOR-R6` | Accepted-contract traceability; Partial-`CR-19` disclosure | Final vocabulary and artifact-currency review |
| `DOR-R7` | Evidence recorded | Jev readiness receipt, then Jev completion receipt for the same pinned scope |

## DoD checklist

**Defined 2026-09-20 (`D-245`) — not complete, not reviewed.** Independent feasibility review by Lane B and
Judge acceptance of the business outcome are each **pending** and recorded separately; neither selects this
MMF nor authorizes construction. DoD is met only by working behaviour and evidence, never by opening or
merging a pull request.

- [ ] **Behaviour, by namespace.** On `ROUTE-PROD-1`: an accepted manual record leads, through the Senior
      Journalist `EW` trigger and `OP-PITCH` classification, to the **`business:T*` judgment records** and the V1
      **`EG*` task/evidence records** — each labelled with its namespace — ending in the `business:T5`
      newsworthiness ranking and routing record (`FR-15`, `AC-23`–`AC-26`, `D-261`; `FR-01`–`FR-04` are not this packet's
      acceptance — `FR-01` only through `AC-02`, `FR-04` only as display provenance). **No criterion here
      promises technical `transition:T*` state execution**
- [ ] **Acceptance cases pass against the real database:** `AC-02`, `SM05-N1`, `SM05-N2`,
      `SM05-N3`, `SM05-N4`, `SM05-N5`, `SM05-N6`, `SM05-RV1`, `SM05-RV2`, `SM05-F1`, `SM05-X1` (`D-260`;
      held technical `T5`/`T6` rows are outside this packet — Option A boundary above), each with its recorded method rationale (`D-242`) and failing-first evidence, or an honest characterization
- [ ] **Refusal and replay:** the `DOR-R2`–`DOR-R4` cases pass — a duplicate `EW` or pitch creates no second
      completion, and missing or unaccepted intake advances nothing
- [ ] **Display, not enforcement:** source-RACI and Line context are displayed with provenance; the Final
      Sign-Off external-tracking reminder is displayed and not executed
- [ ] **Accepted-contract traceability:** the implemented normal and revision journeys, visible
      facts and matching data flow conform to the authoritative `DOR-R5` artifact accepted before
      selection; every displayed fact traces to its governed record/event. The accepted `DOR-R6`
      Encyclopedia disposition remains current at final verification, with no stale vocabulary in
      user-facing or reviewer-facing descriptions. An accepted R6 deferral is verified as a deferral,
      not represented as completed hosted synchronization. *(Added 2026-09-24 from `docs/handoff/B-127`
      Parent 3, accepted by `D-256`.)*
- [ ] **Persistence:** every control and form writes to the database and the UI reflects it; no dead button
- [ ] **Exclusions verified absent:** no `OP-FINAL-SIGNOFF` enforcement, `T6`, article `Published` state,
      Line 3 workflow, external-GRC exchange, WordPress path or `transition:T*` execution claim
- [ ] **Partial-`CR-19` disclosure** appears wherever this slice is described to a user or reviewer
- [ ] **Evidence recorded:** typecheck, lint and tests pass; the full local consistency run (including the
      checks CI skips) passes; migration safe-failure and secret-handling impact are stated. Lane B runs
      Jev completion mode while still `Active` and attaches a `ready-for-independent-review` receipt to its
      handover report (`D-259`) — the receipt is evidence, not closure
- [ ] **Independent verification:** an actor who did not build it confirms the above; the Judge accepts
      the business outcome. **Lane A is that actor (`D-259`)**: after the lock returns it re-runs Jev
      completion mode and runs the customer-acceptance harness (`scripts/acceptance/`); the Judge then
      accepts, and only then does the PR follow its merge act — merge is not DoD (`D-245`)

## Owner / lane

Lane B builds; Lane A tracks and does not cross into `app/`, `lib/`, `components/`, `supabase/`.

## Artifact paths

Not yet created — no code exists for this MMF.

## Test / evidence links

None yet.

## Terminal disposition

Open. Not started; not selected.
