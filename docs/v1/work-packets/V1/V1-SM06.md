# Work packet — `V1` / `V1-SM06` (`MMF-V1-USABLE`)

**Created:** 2026-09-17 (`D-240`, `docs/handoff/B-118`). **Owner:** Lane B (build), Lane A (tracking).
**Status:** Not started; not selected (`D-232`).

**This file is the MMF checklist only.** Scope narrative stays in `V1-BUILD-SPEC.md` and
`requirements-traceability-map.md` §6.1's journey table (steps 9–10) — the owners of those facts.

## Requirement / decision anchors

Second increment — the full PRD success scenario becomes usable. `CR-13`, `FR-08`, `AC-13` (board
visibility), `CR-12`, `FR-09`/`FR-10`, `AC-14`–`AC-16` (publication delivery). `D-239`'s V1 boundary
applies **as amended by `D-243` (2026-09-20)**: this packet may record one LinkedIn target-level
`ManualReady` event — a publication event, not an article state. **Article-state mutation and any article
`Published` state, Final Sign-Off enforcement, and external-GRC exchange remain outside this packet's
scope**, same as `V1-SM05`'s. `FR-10`/`T11` live-URL confirmation and automated WordPress are a V2-target
backlog item, not this packet's work (`D-243`, `D-244`). Board/filter visibility moves here from
`SETUP-SPIKE-000/S3`; the `T6`→`T5` return analytics stay held. Encyclopedia review set: Entry 04.

## Rank

Follows `V1-SM05` — depends on the core flow existing before board/publication usability is
meaningful.

## Dependencies

`V1-SM05` must reach its DoD before this packet's dependencies (board read of state/topic/category/
audit; `ManualReady`/publication delivery) have anything real to display.

## DoR checklist

- [ ] `V1-SM05` reaches its DoD
- [ ] Board/filter scope confirmed against `FR-08`/`CR-13`'s current text (not drafted here)
- [ ] Publication-delivery scope confirmed against `FR-09`/`CR-12` and `D-239` as amended by `D-243`
      (LinkedIn `ManualReady` event only; UI reminder, no enforcement; `FR-10` is a V2 target)
- [ ] `ManualReady` trigger and precondition — **open**, needs a Judge-named fact (`FN-PUBLICATION-09-10-13.md` §12)
- [ ] `ManualReady` acting authority — **open**, needs a Judge-named fact (§12)
- [ ] `ManualReady` revision behaviour — **open**, needs a Judge-named fact (§12)
- [ ] Encyclopedia Entry 04 reviewed against the hosted text (flagged in the sync ledger, not reviewed)

**DoR evidence status (`D-245`, 2026-09-20).** DoR is **defined, not complete.** The first box depends on
`V1-SM05`'s DoD, which has not been earned. Board/filter scope and publication scope still need their
confirmation against the cited requirement text, and the three `ManualReady` children remain open until a
Judge-named fact closes each. No box is checked without current evidence.

## DoD checklist

**Defined 2026-09-20 (`D-245`) — not complete, not reviewed.** Lane B's feasibility review and Judge
acceptance of the business outcome are each **pending** and recorded separately. This packet **cannot be
selected before `V1-SM05` earns its DoD** or receives a separate approved removal/return.

- [ ] **Board:** the board reads and filters state, topic, category and audit trail (`FR-08`, `CR-13`,
      `AC-13`), every filter working against the real database
- [ ] **`ManualReady` ordering, replay, refusal and result** satisfy `FN-PUBLICATION-09-10-13.md` §12: the
      LinkedIn target exists before its append-only event; a repeat yields no second effective outcome and
      stays visible; each refusal names its reason and changes nothing; the UI shows current readiness and
      its evidence
- [ ] **The three open children are decided and met** — trigger and precondition, acting authority,
      revision behaviour
- [ ] **Never `Published`:** no view calls the article `Published` or claims WordPress delivery; the
      `ManualReady` event does not change the article's workflow state
- [ ] **Exclusions verified absent:** no `FR-10`/`T11` confirmation, automated WordPress, `T6`, Final
      Sign-Off enforcement, Line 3 or external-GRC exchange
- [ ] **Persistence:** every control and form writes to the database and the UI reflects it; no dead button
- [ ] **Evidence recorded:** typecheck, lint and tests pass; the full local consistency run passes; secret
      handling is stated (no external-channel credential is used)
- [ ] **Independent verification:** an actor who did not build it confirms the above; the Judge accepts
      the business outcome. Encyclopedia Entry 04 reviewed or explicitly deferred

## Owner / lane

Lane B builds; Lane A tracks.

## Artifact paths

Not yet created — no code exists for this MMF.

## Test / evidence links

None yet.

## Terminal disposition

Open. Not started; not selected.
